// Use ESM export syntax
export default {
  preset: 'ts-jest/presets/default-esm',
  testEnvironment: 'node',
  extensionsToTreatAsEsm: ['.ts'],
  moduleNameMapper: {
    '^(\\.{1,2}/.*)\\.js$': '$1',
  },
  transform: {
    '^.+\\.ts$': ['ts-jest', {
      useESM: true,
      tsconfig: 'tsconfig.json',
    }],
  },
  // Ignore node_modules except for ESM packages that need to be transformed
  transformIgnorePatterns: [
    'node_modules/(?!(node-fetch|fetch-blob|formdata-polyfill|data-uri-to-buffer|fetch-blob/from|formdata-polyfill/esm\\.min\\.js)/',
  ],
  // Add .js to module file extensions
  moduleFileExtensions: ['js', 'ts', 'json'],
  // Setup files after environment is set up
  setupFilesAfterEnv: ['<rootDir>/tests/jest.setup.ts'],
};
