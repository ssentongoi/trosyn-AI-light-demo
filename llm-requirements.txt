# LLM Integration Requirements
# Python 3.10+ required

# Core Dependencies
numpy>=1.24.0
sentence-transformers>=2.2.2  # For local embeddings

# Document Processing
unstructured[all-docs]>=0.10.0  # For document parsing
python-magic>=0.4.27  # File type detection
python-magic-bin>=0.4.14; sys_platform == 'win32'  # Windows support

# Vector Database
chromadb>=0.4.15  # Local vector database
faiss-cpu>=1.7.4; sys_platform != 'darwin' and platform_machine != 'arm64'  # CPU version for most platforms
faiss-cpu-no-avx2>=1.7.4; sys_platform != 'darwin' and platform_machine == 'x86_64'  # For older CPUs
faiss-cpu>=1.7.4; sys_platform == 'darwin' and platform_machine == 'x86_64'  # Intel Mac
faiss-cpu>=1.7.4; sys_platform == 'darwin' and platform_machine == 'arm64'  # M1/M2 Mac

# LLM Runtime
llama-cpp-python>=0.2.0  # Python bindings for llama.cpp

# Utilities
tqdm>=4.65.0  # Progress bars
python-dotenv>=1.0.0  # Environment variables
loguru>=0.7.0  # Enhanced logging

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.0.0
